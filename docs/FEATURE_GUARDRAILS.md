# 23 · 기능 추가 가드레일 (공지)

**목적:** 요구를 반영하되, HTS가 꼬이지 않도록 **한 번에 하나의 Phase**만 엔진/UI에 넣습니다.

---

## 위험 신호 (이면 멈추고 정리 먼저)

| 신호 | 조치 |
|------|------|
| 같은 버튼/열이 **역할이 바뀜** (STOP↔TP/SL) | 문서·UI 라벨 분리, 이전 동작 제거 |
| `23_ENGINE_RULES.md` 와 코드 불일치 | 문서 또는 코드 중 하나를 먼저 고정 |
| 테스트 146개 중 **3건 이상** 연속 실패 | 기능 동결, 회귀만 수정 |
| UI만 추가하고 **엔진 규칙 없음** | `protectionBook` / `oneWayFill` 처럼 **엔진+테스트** 먼저 |

---

## 안전한 추가 순서

1. **엔진 규칙** + 단위 테스트  
2. **문서** (`POSITION_MODE`, `MANUAL_QA`)  
3. **UI** (호가 열 1곳, 토스트)  
4. Gate A (`lint` / `build` / `test` / `smoke`)

---

## 이번 반영 (TP/SL 브래킷)

| 항목 | 범위 |
|------|------|
| OCO | 보호주문 1건 체결 → 같은 `positionId` pending 전부 취소 |
| 호가 TP/SL 열 | 선택 포지션 + 클릭 가격 → TP/SL 자동 판별 |
| MIT/STOP | **왼쪽 STOP만** (변경 없음) |

**하지 않은 것 (다음 Phase):** 실거래 OCO, 부분체결 후 잔량 TP/SL 재계산, 호가에 MIT+TP 혼합 드래그.

---

*이관·WebSocket 없음 — mock HTS 단독 개발.*
